<div class="sm:flex sm:flex-col sm:justify-between">
  <div class="sm:flex-auto">
    <h1 class="font-bold text-2xl leading-6 text-gray-900 dark:text-white"><%= @title %></h1>
    <p class="mt-4 text-sm font-semibold text-gray-700 dark:text-white"><%= @text %></p>
  </div>
  
  <div class="mt-4 sm:ml-16 sm:mt-0 sm:flex-none">
    <%= render LinkComponent.new(title: @link_title, path: @link_path) %>
  </div>
</div>