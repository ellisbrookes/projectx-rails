<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing
    <%= @prodct.name %></h1>

  <%= render "form", product: @product %>

  <%= link_to "Show this project", product_path(@product), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium dark:bg-slate-700 dark:hover:bg-slate-600" %>
  <%= link_to "Back to projects", products_path(@product), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium dark:bg-slate-700 dark:hover:bg-slate-600" %>
</div>
