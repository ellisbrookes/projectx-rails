<%= render HeadingComponent.new(
  title: "Notifications",
  text:
    "A list of the notifications in your account"
) %>

<%= render TableComponent.new(
  headers: ["Content", "Created at"],
  rows:
    @user.notifications.map do |notification|
      [notification.message, notification.url]
    end
) %>
