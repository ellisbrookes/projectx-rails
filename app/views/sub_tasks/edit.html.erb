<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing <%= @sub_task.name %></h1>

  <%= render "form", sub_task: @sub_task %>

  <%= link_to "Show this sub task", company_team_project_task_sub_task_path(@company, @team, @project, @task, @sub_task), class: "ml-2 rounded-lg py-3 px-5 bg-neutral-100 dark:bg-indigo-900 dark:hover:bg-indigo-950" %>
</div>
