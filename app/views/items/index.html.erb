<%= render HeadingComponent.new(
  title: "Items",
  text:
    "A list of the items in your company",
  link_title: "New Item",
  link_path: new_company_item_path
) %>

<%= render TableComponent.new(
  headers: ["ID", "Quantity", "Company"],
  rows:
    @items.map do |item|
      [link_to(item.id, company_item_path(@company, item)),item.quantity, item.company_id]
    end
) %>
