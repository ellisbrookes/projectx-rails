<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing
    <%= @project.title %></h1>

  <%= render "form", project: @project %>

  <%= link_to "Show this project", company_team_project_path(@company, @team, @project), class: "ml-2 rounded-lg py-3 px-5 bg-neutral-100 dark:bg-indigo-900 dark:hover:bg-indigo-950" %>
</div>
